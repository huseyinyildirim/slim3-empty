<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path_for('home') }}">Venigu</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path_for('home') }}">Home <span class="sr-only">(current)</span></a>
            </li>

        </ul>

        <ul class="navbar-nav ml-auto">
            {% if member.check %}
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="far fa-user-circle"></i> {{ member.user.name }}
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path_for('member-changepassword') }}"><i class="fas fa-key"></i> Şifre Değiştir</a>
                    <a class="dropdown-item" href="{{ path_for('logout') }}"><i class="fas fa-sign-out-alt"></i> Çıkış</a>
                </div>
            </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path_for('register') }}">Kayıt Ol</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path_for('login') }}">Giriş Yap</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>